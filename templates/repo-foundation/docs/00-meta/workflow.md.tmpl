# 工作流

## 目的

这份文档定义仓库变更默认遵循的交付流程。

## 默认顺序

标准顺序如下：

`plan -> requirement -> design -> test plan -> implementation -> verification`

## 进入规则

开始实现前，必须先明确：

- 当前活动的 `PLAN-*`
- 需求来源
- 设计说明，必要时补建
- 验证将如何进行

## 退出规则

只有满足以下条件，变更才算完成：

- 计划项被标记为完成
- 最终行为已经被验证
- 如果变更重要，追溯链已同步更新
- 如果受管控文档发生变化，文档地图已同步更新
