# 产品愿景

## 目的

这份文档定义 `{{PROJECT_NAME}}` 的初始产品愿景。

## 愿景

`{{PROJECT_NAME}}` 的目标是：{{PROJECT_DESCRIPTION}}

## 主要目标

- 让任务上下文加载是明确控制的，而不是隐式扩张的
- 让需求、计划、设计和测试都可以被清晰发现
- 减少因大范围或重复加载文档造成的 token 浪费
- 让 Agent 和人类协作者遵守同一套流程
