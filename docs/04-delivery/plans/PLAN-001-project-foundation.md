# PLAN-001 项目基础建设

## 状态

`done`

## 目的

建立一套最小但完整的仓库基础，使项目能够开始进行有纪律的 AI Agent 开发。

## 范围

这份计划覆盖仓库治理、流程定义、产品愿景、AI 加载规则以及质量追溯机制的建立。

## 计划项

| ID | Item | Status | Notes |
| --- | --- | --- | --- |
| P1 | 建立文档地图和仓库约定 | done | `docs/00-meta/doc-map.md`, `docs/00-meta/conventions.md` |
| P2 | 定义规划与实现必须遵守的工作流 | done | `docs/00-meta/workflow.md` |
| P3 | 记录初始产品愿景 | done | `docs/01-product/vision.md` |
| P4 | 定义质量与追溯规则 | done | `docs/05-quality/test-strategy.md`, `docs/05-quality/traceability.md` |
| P5 | 定义 AI 上下文加载策略 | done | `.ai/*` 基础规则文件 |
| P6 | 保留 playbook 作为方法论种子文档 | done | `docs/ai-agent-development-playbook.md` |
| P7 | 启动第一份面向实现的需求或功能计划 | done | `PLAN-002`, `FR-001`, `specs/repository-bootstrap-kit/*` |

## 完成标准

满足以下条件时，这份计划算完成：

- 文档地图中列出的基础文档都已经存在
- 仓库为人类和 Agent 都提供了明确的入口路径
- 下一个功能可以直接挂到新的计划上，而不需要重写治理规则

## 后续承接

基础建设工作已经完成，后续由 `PLAN-002` 承接第一个真实能力建设计划。

## 目的

定义 `FR-001` 的第一版最小实现方式。

## 设计概览

第一版采用“模板目录 + 启动脚本”的形式：

- 在 `templates/repo-foundation/` 下保存基础模板文件
- 在 `scripts/bootstrap_repo.sh` 中实现初始化流程
- 通过简单变量替换生成目标仓库内容

## 关键设计决策

### 决策 1. 先使用 Shell 脚本，而不是引入复杂运行时

原因：

- 当前仓库还没有既定语言栈
- Shell 脚本足够完成目录创建和文本替换
- 可以降低第一次实现的门槛

### 决策 2. 第一版只生成治理骨架，不生成业务代码

原因：

- 当前目标是验证仓库方法是否可复用
- 如果一开始就耦合业务代码模板，会混淆治理能力和业务能力

### 决策 3. 模板以“最小必需文档集合”为边界

第一版仅覆盖 `FR-001.2` 定义的基础文档，避免一次性扩展到完整产品文档体系。

## 拟议目录

```text
templates/
└─ repo-foundation/
   ├─ README.md.tmpl
   ├─ docs/
   │  ├─ 00-meta/
   │  ├─ 01-product/
   │  ├─ 04-delivery/plans/
   │  └─ 05-quality/
   └─ .ai/

scripts/
└─ bootstrap_repo.sh
```

## 输入参数

脚本第一版至少接收以下输入：

- `project_name`
- `project_description`
- `target_dir`

## 处理流程

1. 校验目标目录是否为空或可写
2. 创建标准目录结构
3. 从模板复制基础文档
4. 执行项目变量替换
5. 输出下一步阅读路径

## 风险与约束

- Shell 环境下的文本替换在不同平台上可能有细微差异
- 模板一旦与主仓库文档漂移，就会失去示例价值
- 如果没有覆盖保护，容易误伤已有目录

## 对测试计划的影响

测试需要覆盖：

- 空目录初始化
- 已有文件保护
- 占位符替换正确性
- 生成文档可被工作流直接消费
*** Add File: /Users/mac/code/daxiongAgent/specs/repository-bootstrap-kit/test-plan.md
# 仓库启动包测试计划

## 状态

`active`

## 目的

定义 `FR-001` 第一版实现后的验证方式。

## 测试范围

- 目录初始化行为
- 文档模板生成行为
- 变量替换行为
- 覆盖保护行为

## 测试用例

| ID | 用例 | 目标 |
| --- | --- | --- |
| `TC-001` | 在空目录初始化仓库 | 验证能生成标准治理骨架 |
| `TC-002` | 提供项目名称和简介 | 验证模板变量替换正确 |
| `TC-003` | 目标目录已有关键文件 | 验证不会静默覆盖 |
| `TC-004` | 初始化完成后按 README 路径检查文档 | 验证入口链路完整 |

## 验证方式

- 自动化检查生成出的目录和文件存在性
- 抽样检查模板变量是否被正确替换
- 检查输出结果中是否包含 README、doc-map、workflow、PLAN-001 等关键文档

## 通过标准

- 所有关键文件生成成功
- 占位符未残留在最终文档中
- 覆盖保护符合预期
- 初始化后的仓库可以直接进入下一步计划或需求工作
